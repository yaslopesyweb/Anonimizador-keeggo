# Exemplo de arquivo de variáveis de ambiente
# Copie este arquivo para .env e preencha com suas configurações

# Modelo spaCy a usar (padrão: pt_core_news_lg)
SPACY_MODEL=pt_core_news_lg

# Porta do Streamlit (padrão: 8501)
STREAMLIT_PORT=8501

# Endereço de bind do servidor (padrão: 0.0.0.0 para todos os IPs)
STREAMLIT_SERVER_ADDRESS=0.0.0.0

# Diretório de histórico (onde ficam as transcrições já processadas)
HISTORY_DIR=historico
